<div class="login-box">
  <div class="login-logo">
    <b>Admin</b>LTE
  </div>
  <!-- /.login-logo -->
  <div class="login-box-body">
    <p class="login-box-msg">Connecter vous pour gerer votre association</p>

    {{#em-form model=model submitButton=false}}
      {{#if model.errors.length}}
        {{#each model.errors as |error|}}
          {{error}}
        {{/each}}
      {{/if}}
      {{em-input model=model label="Mote de passe" property="password" type="password"}}
      {{em-input model=model label="Confirmation" property="password_confirmation" type="password"}}
      <div class="row">
        <div class="col-xs-6">
          {{#link-to 'login'}}Se connecter{{/link-to}}
        </div>
        <div class="col-xs-6 pull-right">
          <button type="submit" class="btn btn-primary btn-block btn-flat">Changer de mot de passe</button>
        </div>
      </div>
    {{/em-form}}
  </div>
  <!-- /.login-box-body -->
</div>

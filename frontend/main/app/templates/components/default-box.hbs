<div class="panel panel-default">
  {{#if (not-eq title '') }}
    <div class="panel-heading">
      <h3 class="box-title">{{title}}</h3>
    </div>
  {{/if}}
  <div class="panel-body">
    {{yield}}
  </div>
</div>
